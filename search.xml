<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>单片机笔记</title>
    <url>/2021/05/08/%E5%8D%95%E7%89%87%E6%9C%BA%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p>单片机：</p>
<a id="more"></a>



<p><strong>spi协议：</strong></p>
<p>SPI是串行外设接口（Serial Peripheral Interface）的缩写，是一种高速的，全双工，同步的通信总线，并且在芯片的管脚上只占用四根线，节约了芯片的管脚，同时为PCB的布局上节省空间，提供方便，正是出于这种简单易用的特性，越来越多的芯片集成了这种通信协议，比如AT91RM9200。</p>
<p>SPI总线是一种4线总线，因其硬件功能很强，所以与SPI有关的软件就相当简单，使中央处理器（Central Processing Unit，CPU）有更多的时间处理其他事务。正是因为这种简单易用的特性，越来越多的芯片集成了这种通信协议，比如AT91RM9200。SPI是一种高速、高效率的串行接口技术。通常由一个主模块和一个或多个从模块组成，主模块选择一个从模块进行同步通信，从而完成数据的交换。SPI是一个环形结构，通信时需要至少4根线（事实上在单向传输时3根线也可以）。</p>
<p>SPI的通信原理很简单，它以主从方式工作，这种模式通常有一个主设备和一个或多个从设备，需要至少4根线，事实上3根也可以（单向传输时）。也是所有基于SPI的设备共有的，它们是MISO（主设备数据输入）、MOSI（主设备数据输出）、SCLK（时钟）、CS（片选）。</p>
<p>（1）MISO– Master Input Slave Output,主设备数据输入，从设备数据输出；</p>
<p>（2）MOSI– Master Output Slave Input，主设备数据输出，从设备数据输入；</p>
<p>（3）SCLK – Serial Clock，时钟信号，由主设备产生；</p>
<p>（4）CS – Chip Select，从设备使能信号，由主设备控制。</p>
<p>其中，CS是从芯片是否被主芯片选中的控制信号，也就是说只有片选信号为预先规定的使能信号时（高电位或低电位），主芯片对此从芯片的操作才有效。这就使在同一条总线上连接多个SPI设备成为可能。</p>
<p>接下来就负责通讯的3根线了。通讯是通过数据交换完成的，这里先要知道SPI是串行通讯协议，也就是说数据是一位一位的传输的。这就是SCLK时钟线存在的原因，由SCLK提供时钟脉冲，SDI，SDO则基于此脉冲完成数据传输。数据输出通过 SDO线，数据在时钟上升沿或下降沿时改变，在紧接着的下降沿或上升沿被读取。完成一位数据传输，输入也使用同样原理。因此，至少需要8次时钟信号的改变（上沿和下沿为一次），才能完成8位数据的传输。</p>
<p>时钟信号线SCLK只能由主设备控制，从设备不能控制。同样，在一个基于SPI的设备中，至少有一个主设备。这样的传输方式有一个优点，在数据位的传输过程中可以暂停，也就是时钟的周期可以为不等宽，因为时钟线由主设备控制，当没有时钟跳变时，从设备不采集或传送数据。SPI还是一个数据交换协议：因为SPI的数据输入和输出线独立，所以允许同时完成数据的输入和输出。芯片集成的SPI串行同步时钟极性和相位可以通过寄存器配置，IO模拟的SPI串行同步时钟需要根据从设备支持的时钟极性和相位来通讯。</p>
<p>最后，SPI接口的一个缺点：没有指定的流控制，没有应答机制确认是否接收到数据。</p>
<p>SPI的片选可以扩充选择16个外设,这时PCS输出=NPCS,说NPCS0<del>3接4-16译码器,这个译码器是需要外接4-16译码器，译码器的输入为NPCS0</del>3，输出用于16个外设的选择。</p>
<p><strong>一、ROM</strong></p>
<p>ROM（Read Only Memory)，只读存储器。用来存储和保护数据。</p>
<p>ROM特性：</p>
<p>\1. ROM数据不能随意更新，但在任何时候都可以读</p>
<p>\2. 掉电可保留数据</p>
<p>ROM分类：</p>
<p>\1. PROM：可编程一次性（无法修改的ROM）</p>
<p>\2. EPROM： 紫外线可擦除可编程的ROM</p>
<p>\3. EEPROM： 电可擦除可编程的ROM</p>
<p>a. 按字节进行删除和重写，所以写入时间很长，写入很慢</p>
<p>b. 可以随机访问和修改任何一个字节</p>
<p>c. 目前的EEPROM都是几十千字节到几百千字节的，绝少有超过512k</p>
<p><strong>二、RAM</strong></p>
<p>RAM（Random Access Memory），随机存取存储器。 是与CPU直接交换数据的内部存储器，也叫内存。</p>
<p>RAM特性：掉电不保存</p>
<p>RAM分类：</p>
<p>\1. 静态RAM（static RAM/SRAM）</p>
<p>a. SRAM速度非常快，不需要刷新电路即能保存数据，是目前最快的存储设备</p>
<p>b. 集成度比较低，非常昂贵，多用于一级缓存、二级缓存（L1/L2 Cache）</p>
<p>\2. 动态RAM（Dynamic RAM/DRAM）</p>
<p>2.1 DRAM特点</p>
<p>a. DRAM保留数据的时间很短，需要内存刷新电路，每隔一段时间，刷新充电一次，否则数据会消失</p>
<p>b. 速度比SRAM慢，不过它比任何ROM都要快</p>
<p>c. 价格便宜，内存一般都是DRAM</p>
<p>2.2 DRAM分类（有很多种，其中一种是DDR RAM）</p>
<p>a. DDR RAM（Date-Rate RAM）: 这种RAM可以在一个时钟读写两次数据</p>
<p>内存工作原理：</p>
<p>一个DRAM的存储单元存储的是0还是1取决于电容是否有电荷，有电荷代表1，无电荷代表0。</p>
<p>但时间一长，代表1的电容会放电，代表0的电容会吸收电荷，这就是数据丢失的原因；</p>
<p>刷新操作定期对电容进行检查，若电量大于满电量的1／2，则认为其代表1，并把电容充满电；</p>
<p>若电量小于1／2，则认为其代表0，并把电容放电，藉此来保持数据的连续性。</p>
<p><strong>三、Flash</strong></p>
<p>Flash特性：</p>
<p>\1. 可擦除可编程，跟EEPROM一样</p>
<p>\2. 断电不会丢失</p>
<p>\3. 可以快速读取数据</p>
<p>\4. Flash跟EEPROM最大的区别是Flash按扇区操作，相对于EEPROM的改进就是擦除时不再以字节为单位，而是以块为单位</p>
<p>Flash有两种：</p>
<p>\1. NOR Flash</p>
<p>a. 用户可以直接运行装载在NOR Flash中的代码（主要是因为有XIP技术，片上执行，不像NAND flash得将代码复制到RAM中才可以执行），这样可以减少SRAM的容量从而节约了成本。</p>
<p>b. 地址线和数据线分开，可以实现RAM一样的随机寻址，可以读取任意一个字节</p>
<p>c. 但擦除还是要以块为单位</p>
<p>\2. NADN Flash</p>
<p>a. 数据线和地址线复用，不能利用地址线随机寻址。读取只能按页</p>
<p>总而言之，NOR flash和NAND flash比较，NOR可以支持随机访问，支持XIP，NAND不行；NOR容量小，读的比NAND快；NAND的容量较大，虽然读起来也快；NOR 写擦除都很慢，所以一般放代码段，而NAND写和擦除都很快，所以没有这个应用上的限制。</p>
<p>Flash工作原理：</p>
<p>Flash Memory，属于非易失性存储设备(Non-volatile Memory Device)，Flash的内部存储是MOSFET，里面有个悬浮门(Floating Gate)，是真正存储数据的单元。</p>
<p>数据在Flash内存单元中是以电荷(electrical charge) 形式存储的。</p>
<p>存储电荷的多少，取决于图中的外部门（external gate）所被施加的电压，其控制了是向存储单元中冲入电荷还是使其释放电荷。</p>
<p>而数据的表示，以所存储的电荷的电压是否超过一个特定的阈值Vth来表示。</p>
<p>对于数据的表示，单个存储单元中内部所存储电荷的电压，和某个特定的阈值电压Vth，相比，如果大于此Vth值，就是表示1，反之，小于Vth，就表示0；</p>
<p>对于nand Flash的数据的写入1，就是控制External Gate去充电，使得存储的电荷够多，超过阈值Vth，就表示1了。</p>
<p>而对于写入0，就是将其放电，电荷减少到小于Vth，就表示0了</p>
]]></content>
  </entry>
  <entry>
    <title>啦啦啦</title>
    <url>/2021/05/08/%E5%95%A6%E5%95%A6%E5%95%A6/</url>
    <content><![CDATA[<p>啦啦啦</p>
<p>啦啦啦</p>
<p>啦啦啦</p>
<a id="more"></a>

<p><img src="https://i.loli.net/2021/02/08/thmeckrsDaFYyZf.jpg"></p>
]]></content>
  </entry>
</search>
